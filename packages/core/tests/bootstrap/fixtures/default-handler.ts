/**
 * Default export handler — used to test resolution of handler references
 * without a dot (e.g. "default-handler" → default export).
 */
export default function () {
  return { message: "I am the default!" };
}
